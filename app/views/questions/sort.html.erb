<div class="container">
  <div class="card">
    <h1 class="mx-auto font-custom-title">Paris en cours</h1>
  </div>
  <% @questions_sort.sort_by(&:created_at).reverse.each do |question| %>
    <div class="card-product">
      <%= cl_image_tag question.photo, :class => "question-photo" %>
      <div class="card-product-infos">
        <h2> <%= question.title %></h2>
        <%= link_to 'Détails', question_path(question), class: "btn btn-success" %>
      </div>
    </div>
  <% end %>
</div>
